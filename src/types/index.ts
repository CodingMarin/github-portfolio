import { ReactElement } from "react";

export type Languages = {
  text: string;
  icon: ReactElement;
};

export type Projects = {
  thumbnail: string;
  url: string;
  title: string;
  subtitle: string;
  category: string;
};
